{
  "name": "outlook-mcp",
  "description": "MCP server for a local Windows Outlook (Classic) mailbox: search mail, read threads, calendar, replies owed and awaited, meeting prep, attachments, .msg/.eml parsing; draft_mail / send_mail send only after the user clicks Send in a desktop window. Never moves, deletes or marks anything. Alternative to the outlook skills plugin; install one or the other.",
  "version": "0.3.0",
  "author": {
    "name": "bwinken"
  },
  "keywords": [
    "outlook",
    "email",
    "calendar",
    "mcp",
    "msg",
    "eml",
    "send"
  ],
  "mcpServers": {
    "outlook": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.py"]
    }
  }
}
